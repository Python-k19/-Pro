# ═══════════════════════════════════════════════════════
# Нейро-Озвучка Pro — Зависимости
# code by ТЕХНОПРАНК
# ═══════════════════════════════════════════════════════

# UI фреймворк (современный Tkinter)
customtkinter>=5.2.0

# Синтез речи (нейросетевые голоса Microsoft)
edge-tts>=6.1.9

# Работа с аудио (склейка, эффекты, фильтры)
pydub>=0.25.1

# Воспроизведение аудио в программе
pygame>=2.5.0

# Работа с ID3-тегами MP3 файлов
mutagen>=1.47.0

# Математические операции с аудио данными
numpy>=1.24.0

# Генерация иконок (для сборщика)
pillow>=10.0.0

# Компиляция в .exe (опционально, только для разработчиков)
pyinstaller>=6.0.0

# ═══════════════════════════════════════════════════════
# СИСТЕМНЫЕ ЗАВИСИМОСТИ (не Python-пакеты)
# ═══════════════════════════════════════════════════════
# 
# FFmpeg — для быстрой склейки аудио
# Программа автоматически скачает его при первом запуске
# 
# Ручная установка FFmpeg (если нужна максимальная скорость):
# 
# Windows:
#   1. Скачать с https://www.gyan.dev/ffmpeg/builds/
#   2. Распаковать ffmpeg-release-essentials.zip
#   3. Добавить папку bin в PATH или скопировать ffmpeg.exe в папку с программой
#
# macOS:
#   brew install ffmpeg
#
# Linux (Ubuntu/Debian):
#   sudo apt update
#   sudo apt install ffmpeg
#
# Linux (Fedora):
#   sudo dnf install ffmpeg
#
# Linux (Arch):
#   sudo pacman -S ffmpeg
# ═══════════════════════════════════════════════════════